// Super Runner Game - The original game
class SuperRunnerGame {
    constructor() {
        // This game uses the existing script.js implementation
        // It's already set up, so we just need to ensure it's initialized
        console.log('Super Runner initialized');
    }
    
    cleanup() {
        // Cleanup if needed
    }
}










